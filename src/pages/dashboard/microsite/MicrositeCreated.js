import React, { useState } from 'react';
import { Typography, Checkbox, FormControlLabel, Button, TextareaAutosize } from '@mui/material';

const MicrositeCreated = ({ formData }) => {
  

  return (
    <div>
      <Typography variant="h6">You complete your Microsite request</Typography>
      <Typography variant="body1">We will review the content and after the approval you will receive a notification.</Typography>
    </div>
  );
};

export default MicrositeCreated;
